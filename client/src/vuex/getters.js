const getters = {
  /**
   * getter
   * @param  {[type]} state [description]
   * @return {[type]}       [description]
   * @example
   * getterName (state) {
   *  return state.something
   * }
   */

  status (store) {
    return store.status
  }
}

export default getters
